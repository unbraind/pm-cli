/**
 * @module cli/commands/copy
 *
 * Preserves the CLI import path while copy lifecycle behavior is owned by the SDK.
 */
export {
  runCopy,
  type CopyOptions,
  type CopyResult,
} from "../../sdk/lifecycle/copy.js";
