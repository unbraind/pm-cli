/**
 * @module cli/commands/lifecycle/delete
 *
 * Preserves the CLI import path while the delete transaction is owned by the SDK.
 */
export {
  runDelete,
} from "../../../sdk/lifecycle/delete.js";
