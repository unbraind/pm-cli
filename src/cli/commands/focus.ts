/**
 * @module cli/commands/focus
 *
 * Preserves the CLI import path while focus lifecycle behavior is owned by the SDK.
 */
export {
  runFocus,
  type FocusOptions,
  type FocusResult,
} from "../../sdk/lifecycle/focus.js";
