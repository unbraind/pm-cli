interface:
  display_name: "pm Auditor Compatibility"
  short_description: "Route older reviews to the current pm review skill"
  default_prompt: "Review this tracker using the canonical pm-audit guidance."
