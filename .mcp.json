{
  "mcpServers": {
    "pm-mcp": {
      "command": "node",
      "args": ["./plugins/pm-claude/scripts/pm-mcp-server.mjs"],
      "note": "Native pm-cli MCP server. Uses automatic client/harness identity, the repo build when present, and falls back to npx @unbrained/pm-cli."
    }
  }
}
